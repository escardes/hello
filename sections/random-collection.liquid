{% assign collection = collections['all'] %}

{{ collection }}

{{ collection.title }}
<ul>
  {% for val in collection.products %}
    {% unless section.settings.show-soldout != false and val.available != true %}
      <li> <a  style="color:{{ settings.link_color }}" href = "{{ val.url }}">{{ val.title }}-
        {{ val.price |money }}
        {% if val.available != true %}
          ( SOLDOUT)
        {% endif %}</a>
      </li>
    {% endunless %}
  {% endfor %}
</ul>

{% schema %}
  {
    "name": "Section name",
    "settings": [
      {
        "type":"checkbox",
        "id":"hide-soldout",
        "label":"Hide Sold out Products",
        "default":false
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}